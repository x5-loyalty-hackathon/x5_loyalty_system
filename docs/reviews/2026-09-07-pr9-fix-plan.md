# PR #9: план исправлений и граница игры

Основание: согласованный в чате список исправлений, 07.09.2026.
Входящий визуал — `feat/mobile-polish@30005a6`; изменения вносим в
`experiment/vxofi`, без прямого push в main. Последующие коммиты коллеги
не считаются автоматически просмотренными.

**Уточнение после первого исправления:** `vxofi` согласовал встроенный режим,
в котором внешняя покупка явно моделируется. PoC должен работать из коробки;
отдельный адаптер/консоль не требуются. Это заменяет промежуточное поведение
`fba79ce`, останавливавшее показ сообщением об отсутствии основного приложения.

## 1. Что исправляем

1. Принять текущий визуал PR без перестановки экранов, кнопок и ассетов.
2. Выбор товаров — автоматически сохраняемый в игровой сессии черновик:
   уход на Кухню или просмотр выдачи не удаляет его. Явный выбор другого блюда
   заменяет текущий черновик. Перезапуск и смена синтетического покупателя пока
   сбрасывают сессию; долговременная корзина принадлежит приложению магазина.
3. Существующая нижняя кнопка — «К оформлению». Перед передачей в приложение
   внутренне фиксируем выбранное задание/оффер/SKU. Кнопка не подтверждает чек,
   не запускает готовку и не обещает безусловные 20 XP.
4. Каталог, обычная корзина, адреса, доставка и оплата — экраны приложения
   магазина, не новые игровые экраны. В этом репозитории их нет: добавляем
   интерфейс подключения внешнего оформления. В PoC по умолчанию используется
   его demo-реализация: после явного нажатия с подписью симуляции формируется
   синтетический чек, который проверяет настоящий backend. Новых экранов нет.
   Просмотр корзины/отмена внешнего оформления не создают покупку.
5. Готовка после покупки доступна на Кухне через существующее действие плана.
   Готовое блюдо завершается по подходящему принятому чеку, без готовки.
6. Выбирать и показывать один и тот же готовый SKU, доступный для текущего
   способа получения. Не менять доставку на визит молча. Смена demo-профиля
   не отключает принятую доступность уценки.
7. Повтор после неопределённого сетевого результата использует тот же запрос,
   plan ID и чек. Отмена оформления оставляет выбранное задание и корзину,
   но не считается покупкой или выполнением. Защита XP остаётся серверной.

## 2. Сверка с контрактами

| Документ | Результат сверки |
|---|---|
| ADR-005: задания и XP | Сохраняется: явный выбор + выданный оффер + подходящая покупка + завершение. Для cook подтверждение готовки отдельно; ready закрывает чек. Лимит — один meal-бонус на покупочный день. «Сохранение плана» — внутренний запрос, не обязательная пользовательская кнопка. |
| ADR-004: магазин и книга | Проверка одной точки и fulfillment остаётся. Адрес задаёт основное приложение, отдельного адресного UI в игре нет. |
| Технический дизайн | Промышленная касса, оплата, бронь и интеграция с X5 вне PoC. Внешний обработчик оформления — граница подключения, не заявление о готовой интеграции. |
| branch-merge-plan R1–R3 | Позднее уточнение чата: адреса вне игры; «Подобрать блюдо» принято; отдельная кнопка готового на карточке принята. GET кухни/инструкций — после merge, сюда не включаем. |

Противоречий с действующим правилом XP нет. Прежние предложения добавить
игровое окно оплаты, адреса в игре или последовательность «сохранить → чек →
готовить» на одной кнопке **не реализуем**.

## 3. Проверки перед push

- Реальные обработчики экранов: кнопка оформления не вызывает чек/готовку;
  существующая кнопка на Кухне открывает готовку только при собранных продуктах.
- Черновик переживает навигацию; выбор другого блюда и смена профиля изолированы.
- Смешанные delivery/next_visit SKU; совпадение карточки и выбранного товара;
  профиль A → B → A; уценка не исчезает.
- Оформление: встроенный demo без настройки, явное пояснение до нажатия, отмена внешнего обработчика, успешный чек, потерянный
  ответ и повтор, смена профиля во время запроса; отсутствие лишних XP.
- Client tests, TypeScript, API smoke на mock/model; экспорт; браузерный smoke.
- Эталон исходников обновить на точный PR-коммит, отдельно перечислить только
  согласованные поведенческие/текстовые изменения. Старый визуал не восстанавливать.
- Зафиксировать фактический результат и ограничения, коммитить только файлы
  этого исправления. Незавершённые ML-правки и личные research-файлы не трогать.

## 4. История: реализация и проверка первого исправления

Реализовано в `fba79ce`, документация — `e40ca2f`; входящий PR перенесён
merge-коммитом `b601a6a`. Проверен чистый снимок `fba79ce`, отдельно от локальных
ML-изменений. Backend/recsys по сравнению с PR #9 не менялись.

| Проверка | Результат |
|---|---|
| Полный pytest, Python 3.14 | Пройден: 568 тестов собрано, 3 пропуска optional-тестов, ошибок нет |
| Клиентские тесты | 83/83; включая реальные обработчики экранов и 13 регрессий корзины/внешнего оформления |
| Сквозные TS → API | 16/16 на mock и 16/16 на model; без model fallback |
| TypeScript | Пройден |
| Expo export | Web, iOS, Android — пройден; это не native build |
| Browser smoke, Chromium 390×844 | Отсутствие host, отмена, возврат к корзине, cook, ready, лимит покупочного дня; без JS-ошибок |
| Сохранение визуала | Эталон закреплён на `30005a6`; стили четырёх затронутых экранов идентичны исходнику; слои кухни — 0 изменённых байтов |

При первом browser-запуске временного worktree общий кэш Metro отдал старый UI.
Повтор после `--clear` и явного app root прошёл на финальных исходниках. После
переключения ветки коллегам также стоит запускать Expo с `--clear`, если виден
старый экран. Это не исправлялось изменением продуктового кода.

В первом исправлении ручной показ покупки требовал внешней оболочки. Это
ограничение **снято последующим решением**: [деморежим по умолчанию](../commerce-host.md).
Физический телефон пока не проверен; browser smoke проверяет обычную сборку.

Не включены: незавершённые ML-файлы, новые GET кухни/инструкций, долгоживущая
корзина/история, production-интеграция, косметические изменения будущего PR.

## 5. Встроенный PoC-режим: окончательное поведение

После уточнения `vxofi` включён `demoCommerceHost` по умолчанию. Дополнительная
оболочка, глобальная подстановка и флаги запуска не нужны. До нажатия — явное
пояснение симуляции в существующем `basketHint`; после принятия backend —
«Демо-покупка подтверждена». Ошибки сервера/внешнего обработчика не скрываются
демо-успехом. Внешняя оплата не вызывается, XP остаётся решением сервера.

Локальные проверки нового инкремента: **88/88 client**, **18/18 API mock**,
**18/18 API model**, TypeScript и экспорт web/iOS/Android — пройдены.
Browser smoke 390×844 прошёл **без инъекции host/чеков из теста**, обычными
кнопками приложения: выбор → моделируемая покупка → Кухня → готовка → XP;
ready без готовки и без второго бонуса в тот же день. Пояснение до покупки
проверено на снимке экрана. Проверки отмены и сетевых повторов сохранены.

Визуальное отличие от первого фикса — согласованная demo-подпись. Новых
экранов, кнопок, стилей или ассетов нет. Проверки GitHub выполняются для
обновлённого HEAD PR перед merge; локальный результат не подменяет их статус.
Физический телефон не проверен; для Expo Go нужен адрес backend по LAN.

Коллеге передаётся [подробный handoff](../../mobile/docs/pr9-handoff.md),
включая безопасное обновление его ветки с незавершённой полировкой.
