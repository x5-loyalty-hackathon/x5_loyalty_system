# Интеграция карточек, demo-профилей и метрик — 06.09.2026

## Что изменено

- `mobile-polish@72410ac` влит в `experiment/vxofi` коммитом `1dfef59`:
  карточки `/recipes`, `/recipe`, `/products` без наших визуальных изменений.
- UI1 согласован пользователем: переключатель трёх профилей только внизу
  «Профиля», подпись «Только для демонстрации — синтетические покупатели».
  UI2 (выбор будущей награды) не согласован и не добавлен.
- [API метрик](../metrics-api.md): read-only сводка и дневной ряд для будущих
  дашбордов. Нет нового UI, начислений, production-аналитики или доказанного uplift.
- Visual baseline хранит исходные хеши `72410ac` отдельно от двух явно
  согласованных файлов demo-блока; все ассеты и остальные экраны проверяются как раньше.

## Проверено

- `.venv/bin/python -m pytest -q`: **565 passed, 3 skipped** (568 collected).
- `.venv/bin/python -m pytest -q tests/test_metrics_api.py`: **19 passed**;
  исполнитель отдельно прогнал metrics + rewards + progress/referral + decoration: **118 passed**.
- `cd mobile && npm test`: **66 passed**, спрайты кухни без изменений.
- `npm run typecheck`: успешно.
- `X5_TEST_ENGINE=mock npm run test:api`: **14 passed**.
- `X5_TEST_ENGINE=model npm run test:api`: **14 passed**, настоящий API/model.
- `EXPO_NO_TELEMETRY=1 EXPO_OFFLINE=1 npm run export:check`: web/iOS/Android успешно.
- Chromium, 390×844 и 320×740, настоящий backend с `RECOMMENDATION_ENGINE=model`:
  готовка без докупки; потеря сети и повтор того же плана; нет XP за открытие;
  подтверждение → 20 XP; уценочная докупка/готовка → 40 XP; repeat/ready в тот же
  покупочный день не даёт ещё XP; исходный каталог из десяти закрытых предметов.
- Отдельный браузерный проход demo-блока: семья → овощи → суп → семья;
  meal-запросы идут с правильным user_id, семейные 40 XP восстанавливаются,
  подпись читается на узком экране, на кухне/в подборе переключателя нет.

## Что не заявляем готовым

Физический телефон, сохранение десяти новых предметов на сервере, будущая цель
и подтверждённый баланс наград, результаты конечного LLM-eval и экономики пилота.
Новая ML kitchen/completion-реализация не влита: конфликты с XP-контрактом
зафиксированы в [ревью входящих веток](2026-09-06-incoming-mobile-ml.md).
Личные untracked research-файлы не включены в публикацию автоматически.
