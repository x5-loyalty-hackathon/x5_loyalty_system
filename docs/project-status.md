# Текущее состояние

Последнее обновление: 2026-09-06.

Оперативный остаток до защиты, критерии приёмки и очередь новых коммитов —
[чеклист PoC](poc-readiness.md). Он отделяет обязательные результаты от улучшений
кухни; незакрытые пункты не запрещают публикацию проверенной рабочей ветки.

## Новое решение 06.09: игровые задания и XP

[ADR-005](decisions/005-game-tasks-and-xp.md) фиксирует согласованное `vxofi`
правило: XP за явно выбранное выполненное задание с подтверждённой покупкой,
не за обычный чек и не за одну отметку «приготовил». Три варианта цели используют
current/repeat/explore без новых мини-игр. Вход после кассы сохранён; его нельзя
считать дополнительной покупкой. Demo-предел — один бонус на покупочный день.

**Реализовано в API 1.3:** серверный `offer_id`, проверка выбора/чеков, атомарный
предел и отдельный статус награды; обычный чек и legacy completion дают 0 XP.
Frontend показывает правило до выполнения и объясняет завершение без награды.
Механические дефекты повторов/магазина/радиуса исправлены в `60d4806`.
Проверки и публикация — [план закрытия ревью](reviews/2026-09-06-fixes-and-merge.md).
Исторические описания и результаты интеграции ниже относятся к прежней политике.

## Обновление после PR #8, 06.09

В `experiment/vxofi` объединён `main@372de69` с ML `02132c8`, сохранив API 1.2
и мобильный flow `fc01b94`. В runtime: замороженные 37 обучающих рецептов,
score без сигнала текущего инвентаря, история без будущих чеков, раздельные ID
обучения и фиксированный пул при генерации. Каталог выдачи не ограничен обучением.

Исследовательский стенд теперь тоже в общей истории: несовместимые типы и
политики находятся в [recsys/experimental](../recsys/experimental/README.md).
Четыре исходных манифеста воспроизводятся; отчёты не объявлены оценкой API 1.2.
Номера исследовательских ADR разведены как EXP-002–004; системные ADR неизменны.

Подробности и проверки — [handoff](integration-handoff.md). PR #7 ждёт ревью
ShmelJUJ; main не сливаем автоматически. Защита ветки недоступна на текущем
тарифе приватного репозитория (GitHub 403), доступы/видимость не менялись.

**Ниже сохранена история 05.09; актуальное состояние и границы переноса — выше.**

## Сверка обновлений 05.09

По подтверждению vxofi `experiment/vxofi` — источник истины по системным
решениям. Код коллег адаптируем к API 1.2, ADR-001–004 и
[зафиксированному meal-flow](integration-handoff.md),
включая вход рядом со сценарием доставки до сборки корзины. Старые решения
соседних веток не отменяют уценку, `home`, одну точку и meal-планы.

Поверх интеграции `658ce45` выборочно перенесены кухня/панель/арт из
`feat/mobile-app@4f374c3` и каталог/scorer из `experiment/recsys-benchmark@c28b613`.
Сохранены API 1.2, книга → repeat, одна точка, уценка, cook/ready, планы,
чек выбранных SKU, отдельное подтверждение готовки и личный прогресс.
Исправлен жест панели; ошибка сервера не завершает готовку локально.

Новый ML работает без fallback на mock: 47 рецептов, 98 ингредиентов,
16 признаков. В мобильном запросе пока три фиксированных блюда и один профиль,
без серии исторических чеков; расширение каталога само не расширяет UI-fixture.
Metadata сохранена отдельно от API; вывод остатков дома по истории выключен.
Сравнительный ML-стенд и его отчёты не перенесены целиком, финальный eval открыт.

Локально прошли backend/client regression, TS/API smoke на **обоих** движках,
TypeScript и Expo export всех платформ. Результаты, карта кода, исходные коммиты
и инструкции коллегам — в [памятке интеграции](integration-handoff.md).
Удалённый CI и ручная проверка на телефоне ещё не подтверждены.

Запуск и ручной smoke: [mobile/README](../mobile/README.md).
[Первый план](mobile-integration-plan.md) и
[исходная сверка](reviews/2026-09-05-integration-readiness.md) сохранены как история,
не как актуальный список блокеров: нужные для serving файлы коллег уже получены.

После фиксации интеграции пришли `mobile@b948b37` и `main@8be7a2d`.
Подготовлено объединение для общего main. Наполнение кухни адаптировано: товары
добавляются только после принятия чека API, ошибки и повторы покрыты тестами.
Ready не превращается в сырые ингредиенты; автоматического списания упаковок нет.
Кухня хранит покупки сессии, не точные остатки. Подробнее —
[объединение для main](integration-handoff.md#общий-main-что-добавлено-после-первой-интеграции).

## Сейчас

- Private-репозиторий опубликован в организации `x5-loyalty-hackathon` и
  подготовлен для работы через issues, ветки и pull request.
- Исходная постановка и критерии отделены от будущих командных решений.
- Публичная система X5 Клуба исследована: ядро, персонализация, игровые
  механики, экономика, ограничения и пространство кейса записаны в
  `docs/research/x5-loyalty-current-state.md`.
- Проведено конкурентное исследование: российский grocery, «Золотое Яблоко» и
  другие российские отрасли, международные success/retired/fraud-damaged cases,
  а также экспериментальные работы по отдельным механизмам. Итоговые материалы:
  `docs/research/benchmarks/competitive-case-matrix.md` и
  `docs/research/benchmarks/competitive-synthesis.md`.
- Зафиксирована нулевая гипотеза discovery: ещё одна игра, коллекция или аватар
  сами по себе не гарантируют incremental uplift. Это рамка проверки, а не
  принятое продуктовое решение.
- Команда выбрала meal-first PoC: рекомендации после чека, ingredient-level
  markdown/full-price выбор, доставка или следующий визит и личная кухня
  Домового без gambling/public leaderboard. В той же карточке блюда доступна
  персональная под-опция `Приготовить / Без готовки`; отдельный экран и
  независимая рекламная выдача готовой еды не создаются.
- Первый сегмент — домохозяйства, регулярно покупающие ингредиенты для готовки.
  Ready-heavy пользователи рассматриваются как отдельный анализируемый сегмент,
  а не смешиваются с ним в одной средней метрике.
- Роли распределены на `ML/Recsys & Evaluation`, `Product/UX & Frontend` и
  `Backend/Integration/Safety`.
- Зафиксированы [ADR-001](decisions/001-recipe-first-poc.md),
  [ADR-002](decisions/002-personal-meal-contract.md),
  [technical design](technical-design.md) и
  [план до двух дедлайнов](implementation-plan.md).
- В рабочей ветке реализованы backend-инкременты B1–B3 и meal-контракт:
  versioned
  recommendation contract, deterministic mock, post-model safety, idempotent
  receipt/progress state, private rank, referral reward и rule-based
  precision-first antifraud, а также `cook / ready` рекомендации, сохранение
  meal plan и разное подтверждение завершения двух route. Это ещё не означает
  готовность общего PoC: frontend и pilot evidence принадлежат следующим
  integration gates.
- В API 1.2 замкнут пользовательский цикл `save → repeat`: отдельная
  process-local книга рецептов влияет на следующую рекомендацию без ручного
  эха состояния со стороны frontend. Cook-корзина теперь относится ровно к
  одной точке, выбранной пользователем или рекомендованной по покрытию,
  привычности и расстоянию. Контекст может быть `home`, `work`, текущим или
  произвольным сохранённым местом; demo-дефолт — 750 м без передачи адреса.
- Private rank разделён на `cooking_households` и `ready_heavy`; route одного
  заказа не используется как сегмент.
- Q13 перенесён из рабочего списка вопросов в отдельный
  [одностраничный draft пилота](pilot-plan.md) и включён в план реализации.
- Общий regression gate проходит локально на полном test suite без фиксации
  хрупкого числа тестов в документации. XP-схема подтверждена командой как
  demo-константа; fraud weights и порог ready-affinity всё ещё требуют
  product/model review.
- В общей истории репозитория реализован runnable ML/Recsys smoke-контур поверх
  неизменного backend-контракта: `recsys.model.MLRecommendationEngine`
  (синтетически обучаемый адаптер `RecommendationEngine`, переключаемый через
  `RECOMMENDATION_ENGINE=model`), генераторы профилей, evaluation runner,
  симуляция на 1–10k и экономика. Он нужен, чтобы не блокировать API и smoke,
  но не считается финальной моделью или финальным evidence: профили, labels,
  oracle и funnel rates пока заданы связанными синтетическими правилами.
- Outputs прежнего smoke (`100%` hit rate и для own, и для shuffled history; нулевой
  frequency uplift в base simulation) не интерпретируются как измеренный
  продуктовый результат. Они лишь подтверждают исполнимость пайплайна. Финальные
  методика, профили и результаты остаются deliverable владельца ML/Recsys &
  Evaluation.
- Ветка `feat/mobile-demo` содержит исходный UX-план; код `feat/mobile-app`
  перенесён в нашу ветку. В ней UI теперь использует API 1.2 и плановый
  `cook/ready`, не прежний legacy-маршрут с фиксированным чеком.
- Из актуальной ML-ветки подключены каталог и scorer; сравнительный стенд
  остаётся отдельным. Его card-level LLM-аудит не является согласованным
  симулятором LLM-покупателей с историей и разными персонами. Результаты стенда
  не считаются результатами общей сборки без повторного прогона на API 1.2.

## Следующий командный шаг к MVP

### P0 — блокирует целостный demo

1. **Backend/Integration + Product/UX & Frontend:** пройти подключённый API 1.2
   flow на телефоне и согласовать визуальные правки. Автоматические gates
   закрывают бизнес-переходы, но не мобильную навигацию/вёрстку. Кухонные улучшения
   K1/K2 выделены в чеклисте отдельно; украшения не добавляют новый блокер сдачи.
2. **ML/Recsys + Frontend/Integration:** расширить мобильные данные и персоны
   согласованно с остатками/инструкциями, сохраняя простые три блюда для smoke.
   Работа не блокируется подключением scorer: он уже подключён и проверен.
3. **ML/Recsys & Evaluation + Backend:** адаптировать сравнительный стенд к
   API 1.2 и повторить eval конечной выдачи на 30–50 профилях с baseline и
   раздельным анализом пищевых сценариев. Старые reports не выдавать за новые;
   воспроизводимость integrated scorer уже проверена отдельно.
4. **ML/Recsys + Product:** заменить demo funnel rates на явно согласованный
   набор сценарных допущений и связать выводы с primary metric кейса. До этого
   simulation/economics можно показывать только как работающий калькулятор, а
   не как ожидаемый uplift.

### P1 — обязательные evidence и сдача

5. **Backend/Integration/Safety:** синхронизировать соседние ветки через
   [handoff](integration-handoff.md), сохранить зелёным smoke двух движков и route.
   Передача контракта и examples сделана; следующий новый scope согласовать явно.
6. **Product + review команды:** проверить и зафиналить
   [одностраничный pilot plan](pilot-plan.md): test/control, primary metric,
   margin guardrail, раздельный анализ cooking/ready, длительность и stop rules.
7. **Команда:** записать 30–60-секундное резервное видео полного сценария и
   сверить финальные утверждения в слайдах с фактическими eval/simulation
   outputs.
