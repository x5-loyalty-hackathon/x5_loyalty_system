# Secrets and local configuration
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Operating systems and editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Python
__pycache__/
*.py[cod]
*.egg-info/
.python-version
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# JavaScript / TypeScript
node_modules/
.expo/
.next/
.nuxt/
dist/
build/
coverage/
*.tsbuildinfo

# Logs, local databases and generated output
*.log
*.sqlite
*.sqlite3
artifacts/
outputs/
data/generated/

# Locally-downloaded datasets (e.g. Kaggle CSVs for
# recsys/calibration/fit_from_kaggle.py) — never committed, per
# CONTRIBUTING.md's "no large generated datasets" rule and Kaggle's own
# redistribution terms.
#
# Внимание: шаблон без ведущего слэша совпадает с папкой `data` на любом
# уровне вложенности, поэтому исходники в `*/data/` нужно возвращать явно.
# Git не заходит внутрь исключённой папки, так что возвращать надо сам
# каталог, а не отдельные файлы в нём.
data/
!mobile/src/data/
!recsys/data/

# Notebooks
.ipynb_checkpoints/
