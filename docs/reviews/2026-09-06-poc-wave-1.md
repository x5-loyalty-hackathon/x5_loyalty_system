# Первая параллельная волна после PR #7

Дата: 06.09.2026. База — `experiment/vxofi@17bbe72`, уже включённая в
`main@05cc691`. Эта волна **реализована и проверена локально, ещё не опубликована**.
План и поля решений — [poc-execution-plan.md](../poc-execution-plan.md).

Во время сведения отдельно появился пользовательский коммит `cf140c7` с offline
food.ru-обогащением, данными, package-data и его тестом. Он не является результатом
C1–C4 и не отменялся; наши изменения остались незакоммиченными поверх него.
Приведённый ниже прогон не включает отдельный food.ru-тест.

## Результат

| Участок | Выполнено | Где проверить |
|---|---|---|
| C1, агент ML-integration | Перенесены совместимые fixes приглашений, ready-пар, расчёта коэффициента цен, effort registry и наблюдаемости каталога. Serving API 1.3 и XP сохранены; изменения исследовательской методики оставлены D3 | [Подробный отчёт SHA → файлы](2026-09-06-poc-ml-fixes.md) |
| C2, агент Mobile-personas | Три cooking-профиля с четырьмя прошлыми чеками, переключатель в существующих экранах, отдельные user/receipt IDs; изоляция книги, плана, покупок и XP, защита от поздних ответов и A→B→A | [Mobile README](../../mobile/README.md), `mobile/tests/profiles.test.mjs`, `mobile/tests/integration/api-flow.test.mjs` |
| C3, агент Safety-demo | Автономный реальный API-сценарий: первая покупка без XP, referral20+20, повторы, self/replay/mismatch, неоднозначные device/payment, отдельные личные когорты | [Инструкция](../safety-demo.md), `scripts/safety_demo.py`, `tests/test_safety_demo.py` |
| C4, основной агент | Экспорт конечной meal-выдачи на 30–50 synthetic-профилях; полные запросы/ответы, пустая таблица оценок, manifest/hashes, отказ перезаписывать чужую разметку | [Инструкция и ограничения](../serving-review.md), `scripts/serving_review_pack.py`, `tests/test_serving_review_pack.py` |

Каждый агент работал в выделенных файлах без commit/push. Основной агент
просмотрел изменения и повторил проверки общей рабочей копии; это не новое
независимое blind-review и не ручной прогон телефона.

## Общая проверка после сведения

```bash
.venv/bin/python -m pytest --ignore=tests/test_foodru_offline.py -o addopts='' -q --tb=short
cd mobile
npm run typecheck
npm test
npm run test:api
X5_TEST_ENGINE=model npm run test:api
CI=1 EXPO_NO_TELEMETRY=1 EXPO_OFFLINE=1 npm run export:check -- --max-workers 2
```

- **445 Python passed, 3 skipped** (Python 3.14, 107.95 с).
- **34 client passed; 12 mock + 12 model API passed**, fallback запрещён тестами.
- TypeScript и Expo export **web/iOS/Android** прошли.
- `git diff --check` — без ошибок.
- `tests/test_foodru_offline.py` намеренно исключён: это параллельная
  пользовательская работа, не часть C1–C4. Три штатных skip внутри нашего набора:
  два optional booster, один fixture без сохранённых рецептов.
- Новые проверки GitHub CI не запускались: изменений этой волны ещё нет в origin.

## Результат экспортирующего сценария — не оценка релевантности

Выполнено два новых изолированных запуска `scripts.serving_review_pack`:
40 профилей, seed606, один из запусков с `PYTHONHASHSEED=42`.
`responses.json` совпал побайтно (`cmp`, exit0).

- Распределение генератора: routine21, value7, explorer3, time_limited9.
- **28 профилей с результатом; 12 пустых выдач; всего 43 блюда**.
- Все пустые случаи сохранены. Это покрытие synthetic-выдачей, **не 70% качества**.
- `cases_sha256`: `3e7347ce920d61a0a9633a4b8734c3a6581541071098c3e60973b26ee4e06210`.
- Метки эксперта пусты, `quality_status=not_assessed`. Ready-SKU отсутствуют
  в используемом генераторе остатков; ready-eval этим пакетом не закрыт.
- Пакет проверяет текущие 47 serving-рецептов, не три mobile-рецепта и не новый
  baseline для исследований. Готовые исследования коллег не переименовывались.

Фактические различия трёх мобильных профилей и контроль замены только истории
зафиксированы в Mobile README. Они показывают подключение персонализации,
не экспертное качество или влияние на покупки.

## Что дальше и что не было решено автоматически

- C5 / P1: живой интерфейс, жесты/прокрутка/ошибки и резервное видео.
- D1–D2 → C6: конкретное оформление и правило его открытия. Новых косметических
  предметов, порогов, расходуемой валюты и реакций не реализовано без выбора.
- D3–D4 → C7: методика окончательной оценки, разбор пустых выдач, baseline,
  экономические допущения и финальные результаты. Симуляция не стала causal AB.
- P6: финальные материалы, Q13/pilot plan и проверка доступа жюри.
- Дальнейшая публикация — отдельный проверенный инкремент; PR #7 относится к базе,
  его зелёные проверки не приписываются этой локальной волне.

Чужие `pyproject.toml`, `recsys/data/foodru/`, `recsys/offline/`,
`tests/test_foodru_offline.py` и личные research-файлы не изменялись нами и
не включались в commit. Файлы и ответы пользователя не перезаписывались.
