# Эксперимент 2: влияние сервисной логики

Сгенерирован `python -m recsys.experiment2_service_logic`. Один `MLRecommendationEngine()` (не переобучается между вариантами), панель `development/train` — 603 пользователей × 3 уровня дефицита × 4 варианта обработки, 101 с.

Популяция берётся из `recsys.panels`, а не генерируется здесь: профили, истории и выкладки материализованы и захешированы, поэтому повторный прогон доказуемо видит те же данные, а уровни дефицита доказуемо отличаются только полкой — один и тот же хеш профилей, разные хеши инвентаря. Сплит `train`, потому что этот эксперимент **выбирает** политику, а выбор делается на train (`docs/research/recsys/evaluation-protocol.md` §3).

Поведенческие миры (`recsys.regimes`) здесь намеренно не разворачиваются. Эксперимент отвечает «какая рука лучше на нашей популяции»; «переживёт ли это другого покупателя» — вопрос устойчивости, и им владеет `recsys.sensitivity`. Разделение даёт одну большую правильно разбитую популяцию вместо девяти маленьких неразбитых — и делает возможным парное сравнение по пользователям.

**Вопрос:** зафиксировав `model_score`, что меняет именно обработка выдачи в `app.service`? Это не эксперимент про качество ранкера (см. эксперимент 1) — здесь ранкер один и тот же, отличается только то, что `RecommendationService`/`RankingPolicy` делают с уже посчитанными кандидатами: пересортировать, отбросить или не трогать.

---

## 0. Методология

- **Панель.** `recsys.panels`, сплит `development/train`, 603 пользователей. Материализована, захеширована и проверяется при загрузке; уровни дефицита получены через `Panel.with_inventory`, который сохраняет seed инвентаря — те же случайные числа, другие пороги, то есть парное сравнение по общим случайным числам, а не два независимых розыгрыша.
- **Единица сравнения — пользователь, а не ячейка.** Каждую руку видит один и тот же человек с той же корзиной и той же полкой, поэтому руки вычитаются по-пользовательски, а доверительный интервал строится бутстрепом по пользователям. Прежняя формулировка «доля выигранных ячеек» была навязана девятью маленькими популяциями и, кроме того, не является размером эффекта.
- **Что меряется — на финальной выдаче, а не на сырых кандидатах.** Каждый вариант вызывает `RecommendationService.recommend()` целиком; `oracle_relevant` судит `RecommendationResponse.recommendations` (то, что реально показано пользователю в топ-3), а не `engine.rank()` — это и есть отличие от эксперимента 1.
- **Уровни дефицита.** `no_product_at_all`/`out_of_stock` на low/base/high теми же откалиброванными значениями, что в `recsys/sensitivity.py` (`DIALS`): 0.02/0.08/0.35 и 0.02/0.10/0.35 соответственно, применены как одна общая ось «скудности полки» (на low оба дана на нижнем значении, на high — оба на верхнем), а не как независимая развёртка по каждому диалу — иначе сетка выросла бы до 9 сочетаний вместо 3, а вопрос эксперимента про это не спрашивает.
- **Порог `feasibility_missing_cap = 6`.** Выбран эмпирически как p75 `missing_count` по всем собранным (до сортировки и обрезки) кандидатам на этой же панели, на уровне дефицита «база» (603 пользователя): p50=4, **p75=6**, p90=8, максимум 14. Осознанно ниже `MAX_EFFORT_MISSING_COUNT` (8, константа шкалы «максимального усилия» в `app.service`) — цель этого варианта не подрезать хвост, а отсечь дороже-докупаемую четверть *уже собираемых* рецептов как нереалистичную к покупке сегодня.
  Порог откалиброван на одном уровне дефицита, а применяется на всех трёх, и уровень заметно двигает распределение: низкий — p50=5/p75=7/p90=9/max=17, база — p50=4/p75=6/p90=8/max=14, высокий — p50=3/p75=4/p90=5/max=9. То есть 6 отсекает примерно верхнюю четверть на базе, режет строже p75 при низком дефиците и почти не срабатывает при высоком, где скудная полка уже убрала дорогие варианты.
  До 2026-09-06 этот пункт приписывал числа выборке `REGIMES[:9]`, 80 польз./мир. Это неверно: эксперимент 2 работает на панелях (`recsys.panels.experiment_panels`), которые по своему устройству поведенческие миры не свипуют вообще. Перепроверено — процентили воспроизводятся на панели точно, так что само значение было верным, ошибочной была только атрибуция, скопированная из паспорта эксперимента 1.
- **Метрика релевантности.** `precision@k` = доля показанных карточек (после сборки и обрезки до топ-3), которые `oracle_relevant` считает релевантными — не доля от всех кандидатов модели. Рядом — `hit_rate` (доля пользователей, у кого хотя бы одна карточка в топ-3 релевантна), т.к. одно число легко перепутать с другим.
- **Стоимость докупки.** Сумма цен реально выбранных `ProductOption` (первый по `_valid_products`-сортировке вариант на каждый недостающий ингредиент — то, что сервис фактически подставил бы в корзину), не абстрактная `BASE_PRICE_RUB`.

---

## 1. Итоговые числа по вариантам и уровням дефицита

### низкий

| Вариант | precision@k | hit_rate | доля missing=0 | ср. missing_count | ср. докупка, ₽ | доля пустых выдач |
|---|---:|---:|---:|---:|---:|---:|
| 1. model_order (чистый порядок модели) | 0.739 | 0.891 | 0.003 | 3.95 | 472 | 0.036 |
| 2. effort_first (шипанное поведение) | 0.694 | 0.877 | 0.003 | 3.57 | 424 | 0.036 |
| 3. feasible_model_order (отсечь неосуществимое) | 0.743 | 0.872 | 0.003 | 3.57 | 428 | 0.048 |
| 4. blended (мягкий учёт доступности) | 0.712 | 0.886 | 0.003 | 3.61 | 429 | 0.036 |

### база

| Вариант | precision@k | hit_rate | доля missing=0 | ср. missing_count | ср. докупка, ₽ | доля пустых выдач |
|---|---:|---:|---:|---:|---:|---:|
| 1. model_order (чистый порядок модели) | 0.699 | 0.784 | 0.001 | 3.85 | 461 | 0.075 |
| 2. effort_first (шипанное поведение) | 0.674 | 0.783 | 0.001 | 3.65 | 437 | 0.075 |
| 3. feasible_model_order (отсечь неосуществимое) | 0.712 | 0.766 | 0.001 | 3.49 | 425 | 0.093 |
| 4. blended (мягкий учёт доступности) | 0.681 | 0.784 | 0.001 | 3.67 | 440 | 0.075 |

### высокий

| Вариант | precision@k | hit_rate | доля missing=0 | ср. missing_count | ср. докупка, ₽ | доля пустых выдач |
|---|---:|---:|---:|---:|---:|---:|
| 1. model_order (чистый порядок модели) | 0.697 | 0.327 | 0.021 | 2.97 | 361 | 0.580 |
| 2. effort_first (шипанное поведение) | 0.694 | 0.327 | 0.021 | 2.96 | 357 | 0.580 |
| 3. feasible_model_order (отсечь неосуществимое) | 0.701 | 0.323 | 0.022 | 2.85 | 347 | 0.584 |
| 4. blended (мягкий учёт доступности) | 0.699 | 0.327 | 0.021 | 2.96 | 357 | 0.580 |

Пустая выдача (`recommendations == []`) ожидаемо возможна только у варианта 3 — это единственный, кто вообще что-то отбрасывает после сборки; у остальных трёх пустая доля должна быть равна доле пользователей, для которых сервис не собрал вообще ни одного кандидата (безопасность/эксклюзии), а не доле, связанной с политикой ранжирования.

---

## 2. Варианты 1/3/4 против шипанного (`effort_first`)

Разбивка по 9 мирам, доля побед — это не p-value: миры выбраны нами, чтобы накрыть диапазон, не сэмплированы из реальности.

### низкий

- **1. model_order (чистый порядок модели)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.046, медиана +0.046, p90 +0.046
- **1. model_order (чистый порядок модели)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +47.750, медиана +47.750, p90 +47.750
- **3. feasible_model_order (отсечь неосуществимое)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.049, медиана +0.049, p90 +0.049
- **3. feasible_model_order (отсечь неосуществимое)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +4.066, медиана +4.066, p90 +4.066
- **4. blended (мягкий учёт доступности)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.018, медиана +0.018, p90 +0.018
- **4. blended (мягкий учёт доступности)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +5.695, медиана +5.695, p90 +5.695

### база

- **1. model_order (чистый порядок модели)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.025, медиана +0.025, p90 +0.025
- **1. model_order (чистый порядок модели)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +23.823, медиана +23.823, p90 +23.823
- **3. feasible_model_order (отсечь неосуществимое)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.038, медиана +0.038, p90 +0.038
- **3. feasible_model_order (отсечь неосуществимое)** vs `effort_first`, `ср. докупка, ₽`: побед в 0% миров (1 миров), разброс дельты p10 -12.682, медиана -12.682, p90 -12.682
- **4. blended (мягкий учёт доступности)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.007, медиана +0.007, p90 +0.007
- **4. blended (мягкий учёт доступности)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +2.778, медиана +2.778, p90 +2.778

### высокий

- **1. model_order (чистый порядок модели)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.003, медиана +0.003, p90 +0.003
- **1. model_order (чистый порядок модели)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +4.012, медиана +4.012, p90 +4.012
- **3. feasible_model_order (отсечь неосуществимое)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.007, медиана +0.007, p90 +0.007
- **3. feasible_model_order (отсечь неосуществимое)** vs `effort_first`, `ср. докупка, ₽`: побед в 0% миров (1 миров), разброс дельты p10 -9.818, медиана -9.818, p90 -9.818
- **4. blended (мягкий учёт доступности)** vs `effort_first`, `precision@k`: побед в 100% миров (1 миров), разброс дельты p10 +0.005, медиана +0.005, p90 +0.005
- **4. blended (мягкий учёт доступности)** vs `effort_first`, `ср. докупка, ₽`: побед в 100% миров (1 миров), разброс дельты p10 +0.033, медиана +0.033, p90 +0.033

---

## 3. Устойчиво ли это к уровню дефицита

Сравнение направления вывода (кто впереди по `precision@k`) между уровнями дефицита — переворот не шум, а находка, которую нужно называть явно.

| Вариант | precision@k: low → base → high |
|---|---|
| 1. model_order (чистый порядок модели) | 0.739 → 0.699 → 0.697 |
| 2. effort_first (шипанное поведение) | 0.694 → 0.674 → 0.694 |
| 3. feasible_model_order (отсечь неосуществимое) | 0.743 → 0.712 → 0.701 |
| 4. blended (мягкий учёт доступности) | 0.712 → 0.681 → 0.699 |

---

## Угрозы валидности

- **Уровни дефицита совпадают по числам с `recsys.sensitivity.DIALS`, но не переиспользуют модуль напрямую** — построены как отдельные `InventoryAssumptions` (`no_product_at_all`/`out_of_stock` на 0.02/база/0.35), тем же способом, что `recsys.sensitivity`/`recsys.benchmark` уже используют. Более ранняя версия скрипта пыталась достичь того же через monkeypatch модульных констант `recsys.inventory.P_NO_PRODUCT_AT_ALL`/`P_OUT_OF_STOCK` — это не работает, потому что `DEFAULT_INVENTORY_ASSUMPTIONS` строится один раз при импорте модуля и не перечитывает глобальные константы при каждом вызове; три «уровня дефицита» в той версии считали одно и то же. Исправлено до публикации этого отчёта.
- **Два дефицит-диала двигаются вместе**, не по отдельности, как в `recsys/sensitivity.py`. Если `no_product_at_all` и `out_of_stock` тянут вывод в разные стороны, эта развёртка их не различит.
- **`oracle_relevant` не знает про режим (`Regime`).** Он читает `recsys.profiles.ARCHETYPES` напрямую (глобальные, не изменённые дилами новизны/промо/повтора), тогда как сами популяции генерируются через `regime.archetypes()`. Это унаследованное от `recsys.evaluation`/`recsys.response_models.OracleResponder` свойство, не что-то специфичное для этого эксперимента — но означает, что «релевантность» здесь мягче реагирует на дилы `novelty`/`promo`/`repeat`, чем сама генерация профилей.
- **`oracle_relevant` не гейтит на осуществимость** (см. паспорт метрики 0.19 в плане экспериментов) — он же используется как относительный инструмент сравнения рангов, self-referential относительно `recsys.model`, не независимая истина о желании готовить.
- **`FEASIBILITY_MISSING_CAP = 6` — наш выбор, не измеренная константа.** Он подобран по p75 распределения на этой конкретной панели; другая панель (другой каталог рецептов, другая смесь архетипов) дала бы другое число.
- **Cтоимость докупки — оценка** (`price_is_estimate`), откалиброванная по распределению цен X5, но не поингредиентно истинная.
- **Миры выбрали мы.** Сетка режимов — диапазон стресса, а не выборка из реальности; доля побед по мирам — не p-value.

