# Что осталось до PoC и финальной защиты

Обновлено 06.09.2026. Финальная сдача — **07.09, 10:00 МСК**.
Основа: решения `experiment/vxofi`, API 1.3, ADR-001–005; первая интеграция
`1e55cd7`, `fc01b94`, затем обновление новым `main@372de69` в PR #7.
Это рабочий чеклист по итогам обсуждения, не новый список продуктовых требований.
**Незавершённый PoC не запрещает push проверенных промежуточных изменений.**

## Новый инкремент 06.09: XP за игровое задание

- [x] **XP-contract:** [ADR-005](decisions/005-game-tasks-and-xp.md) фиксирует
  основное согласованное правило и три варианта цели без новых мини-игр.
- [x] **XP-limit:** один meal-бонус на подтверждённый покупочный день —
  консервативный demo-параметр при реализации разрешённого плана.
- [x] **XP-implementation:** выбранное задание связано с серверным оффером и
  подходящими чеками, награды отделены от завершений, passive purchase XP убран;
  UI адаптирован. Сценарии §9, включая вход после кассы и retry, автоматизированы.

Это реализованная часть API 1.3. Исторические smoke в старых записях относятся
к прежним правилам XP. Новая политика не требует дополнительных
экранов, production-auth, мини-игр или полного магазина косметики.

## Уже работает

- Вход перед сборкой доставки или после недавней покупки → подбор блюда с
  объяснением → `cook/ready` → товары/уценка одной точки → план → чек → прогресс.
- Сохранение рецепта и следующий `repeat`; готовка подтверждается отдельно от
  покупки, а готовое блюдо завершается чеком. Повтор запроса не удваивает XP.
- Новая кухня, панель и шаги готовки; ошибки не маскируются локальным успехом.
- ML scorer подключён: 37 baseline-рецептов для обучения, 47 в общем каталоге,
  16 признаков (сигнал инвентаря выключен в score по умолчанию); мобильный
  запрос пока содержит один фиксированный профиль, три блюда и пустую историю чеков.
- Backend рефералов, личной позиции и антифрода реализован и покрыт тестами.
- Симуляция/экономика исполняются; одностраничный план пилота есть в draft.

Результаты проверок и карта кода: [integration-handoff.md](integration-handoff.md).
Наличие работающего кода не означает доказанное качество рекомендаций или uplift.

## Остаток: шесть проверяемых пунктов

Очередь после исправления ревью и merge PR #7 (план, не отметка о выполнении):

1. **P1: один живой показ API 1.3.** Чистый старт, cook с докупкой, ready,
   вход после кассы, завершение без XP, повтор потерянного ответа. Сначала
   убедиться, что на устройстве видны новые условия и нет старых 10/30 XP.
2. **P2 и сверка свежей ML-ветки.** На 06.09 опубликована
   `experiment/recsys-benchmark-clean-authorship@70f3af5`, тогда как текущая
   интеграция ML основана на `02132c8`. Проверить её изменения отдельно, не
   подменяя новые API/XP старым кодом; переносить только необходимые fixes.
   Затем подключить несколько осмысленных профилей к UI вместо одного fixture.
3. **P3/P4/P6 параллельно по прежним зонам ответственности:** оценка конечной
   выдачи, воспроизводимая экономика и финальные материалы. Повторно проверить
   актуальность ответов коллег, не объявлять их старые отчёты финальным evidence.
4. **P5:** компактный демонстрационный сценарий referral/antifraud/private rank
   с новыми нулевыми passive XP и неизменной отдельной реферальной наградой.
5. **Следующий игровой инкремент:** минимальный выбор оформления за общий
   прогресс и различимая реакция Домового на задание. Это открытая часть
   продуктового обещания ADR-005, не реализованная косметика. Сначала оценить
   объём с Product/Frontend; новые мини-игры, валюта и полный магазин не требуются.

Это порядок ближайшего разбора, не новое назначение ролей и не автоматическое
разрешение расширять реализацию. Точные критерии P1–P6 остаются в таблице ниже.

Роли ниже — ранее согласованные зоны, без нового назначения участников.
Закрываем пункт ссылкой на результат, а не только отметкой «сделано».

| ID / статус | Что закрыть | Минимальная приёмка | Основной участок |
|---|---|---|---|
| **P1 ☐** | Ручной прогон интерфейса | На целевом телефоне/браузере пройти cook, ready, repeat, смену места и ошибку сети; проверить панель/прокрутку. Сохранить замечания и короткое видео. Автотесты не заменяют этот прогон | Frontend + Integration |
| **P2 ☐** | Персонализация в демо | Несколько переключаемых синтетических профилей с разной историей; показать осмысленные различия предложений/объяснений. Все 47 рецептов переносить в UI необязательно; не смешивать ready-heavy и готовящих в одной оценке | ML + Frontend/Integration |
| **P3 ☐** | Финальная оценка рекомендаций | 30–50 профилей, заранее описанный критерий релевантности, baseline, результаты и ошибки; проверить ориентир кейса ≥70%. Оценивать окончательные предложения API 1.3 после safety/selector, а не только модель | ML/Evaluation + Backend |
| **P4 ☐** | Симуляция и экономика для защиты | Воспроизводимый отчёт на 1–10 тыс. пользователей: допущения, пессимистичный/базовый/оптимистичный сценарии, дополнительные покупочные дни и маржа после издержек/каннибализации. Не называть заданные вероятности измеренным эффектом | ML/Evaluation + Product |
| **P5 ☐** | Демонстрация остальных требований | Воспроизводимо показать успешное приглашение, повтор без награды, подозрительный чек/приглашение и изменение личной позиции. Логика уже есть; допустим компактный API-сценарий, отдельные большие UI-экраны не обязательны | Backend/Safety + Frontend для представления |
| **P6 ☐** | Финальный комплект и доступ | Финализировать pilot plan, Markdown-описание, около трёх продуктовых материалов, презентацию с фактическим вкладом участников; резервное видео, опубликованный код и проверенный доступ жюри | Product + вся команда |

Уточнения к приёмке:

- **P3:** текущий `recsys/evaluation.py::run_evaluation` берёт
  `engine.rank(...)[:top_k]`, не конечный ответ `RecommendationService`.
  Его rule-based оценщик связан с генератором синтетических предпочтений.
  Это существующий диагностический тест, не готовое независимое доказательство
  качества всего продукта. Новую методику/отчёт готовим явно, старые числа не переименовываем.
- **P4:** `recsys/simulation.py` моделирует воронку по заданным вероятностям,
  не проигрывает рекомендации каждому из 10 тыс. покупателей. В базе дополнительный
  покупочный день не предполагается. Рост открытий, смена канала и докупка в уже
  запланированный день не доказывают рост частоты. Нулевая денежная награда не
  обнуляет издержки работы механики.
- **P5:** неоднозначный общий device/payment → проверка, не автоматическое
  обвинение. Пороги — объяснимые demo-правила; численный precision на реальных
  данных не заявляем. Личная позиция не требует публичного списка покупателей.
- **P6:** [pilot-plan.md](pilot-plan.md) уже содержит test/control, покупочные
  дни и маржинальное ограничение. Нельзя выдумать размер выборки/MDE без данных X5;
  такие параметры остаются явно обозначенными условиями запуска реального пилота.

## Усиление игровой концепции — отдельно от обязательной сдачи

- [x] **K1: подтверждённая покупка → продукт появляется на кухне.** Реализовано
  при объединении с актуальным main: начальный чек плюс принятые API покупки,
  без повторного добавления от retry и без ложного успеха при ошибке/отклонении.
  Ready остаётся готовым блюдом; после готовки целые упаковки не списываются.
  Автотесты проходят, внешний вид ещё требует ручной проверки P1.
- [ ] **K2: «закончилось».** Полезная поправка пользователя к предположению о
  наличии дома; полноценный склад, сроки годности из истории и автоматическое
  списание целых упаковок после готовки не требуются.

Декор/сложное развитие дома не расширяем до закрытия основного показа и evidence.
Эти улучшения не становятся молча дополнительными критериями кейса.

## История 06.09 до API 1.3: main включил ML-ветку

После PR #8 интеграционная ветка обновлена до `main@372de69` / ML `02132c8`.
API 1.2 и mobile сохранены. Исправления обучения подключены, исследовательские
политики отделены от serving, исходные ML-тесты сохранены. Четыре манифеста
воспроизводятся без изменения. [Карта переноса](integration-handoff.md).

Это **не закрывает P3/P4**: отчёты старого стенда не измеряют итоговую выдачу
приложения. Ручной UI-прогон также открыт. Прежнее ожидание отдельного ревью
ShmelJUJ заменено по указанию `vxofi` двумя независимыми агентами;
актуальный порядок merge — в [плане закрытия ревью](reviews/2026-09-06-fixes-and-merge.md).

## История 05.09: объединение mobile и нашей интеграции

При сверке 05.09 получен `feat/mobile-app@b948b37`; ML остаётся `c28b613`.
В `main@8be7a2d` уже слита обновлённая mobile-ветка. По запросу пользователя
подготовлено её объединение с нашей адаптацией API 1.2 для публикации общего main.

Включено после `1e55cd7` с адаптациями, перечисленными в handoff:

- `6c70677`: наполнение кухни покупками, граница видимости слотов, запасные спрайты;
- `f825be7`: убраны неподтверждённые подписи/индикаторы с кухонных карточек;
- `b948b37`: уточнение ignore-правила для каталогов `data`.

Для **K1 использована работа коллеги как исходник**. В исходном `DemoContext.confirmPurchase` кухня
пополняется и корзина очищается **до** запроса/вердикта сервера; это не переносим
как подтверждённую покупку. Адаптация к meal-плану и регрессии
`verified / duplicate / rejected / network error` реализованы. Запасной спрайт по размеру
не гарантирует совпадение с продуктом — сохранять понятные подписи.

## Порядок ближайших действий

1. Проверить объединённую версию, слить в main и передать команде ссылку на handoff в main.
2. **P1 → P2**; K1 уже включён в объединение, следующий полезный кандидат — K2.
3. Параллельно **P3/P4**; к защите закрыть **P5/P6**.
4. Последующие изменения делать небольшими коммитами. Новые коммиты соседней
   ветки фиксировать в очереди интеграции, не откладывая каждый push до нового
   полного merge. В `main` — отдельная согласованная интеграция, не автоматический push.

Новая архитектура, production-интеграции, дополнительные модели и полноценный
холодильник не нужны для замыкания PoC. Личные research-файлы не включаем в
публикацию автоматически; актуальные решения и критерии должны читаться здесь
и в ADR без истории чата.
