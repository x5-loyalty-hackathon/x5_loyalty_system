# Как собрана общая ветка: frontend + ML

Статус 07.09: визуал PR #9 `mobile-polish@30005a6` и согласованные поведенческие
исправления. [План/сверка](reviews/2026-09-07-pr9-fix-plan.md),
[контракт оформления и ограничения автономного показа](commerce-host.md).
Ручной прогон на телефоне и финальный ML-eval ещё нужны. Точка входа для коллег, которым
нужно понять изменения без истории чата. Источник системных решений —
`experiment/vxofi`. Исполняемый API — **1.3**, ADR-001–005;
ниже новое начисление XP отделено от исторической интеграции API 1.2.

Первая выборочная интеграция: **`1e55cd7`**. Следом подготовлено объединение с
`main@8be7a2d`, включающим `mobile@b948b37`; отличия описаны ниже.
Остаток до PoC — в [оперативном чеклисте](poc-readiness.md).

## Последняя интеграция: PR #9

Стили, ассеты и порядок экранов `30005a6` сохранены. В четырёх экранах изменены
только согласованные обработчики/подписи/доступность; исключения записаны в
`mobile/tests/fixtures/mobile-polish-visual-baseline.json` отдельно от хешей PR.
План перед оформлением фиксируется внутренне, «К оформлению» вызывает обработчик
основного приложения, готовка живёт на Кухне. Готовый SKU фильтруется по текущей
доставке/визиту, уценка сохраняется после смены профиля, корзина — при навигации.

**Основного приложения магазина в репозитории нет.** Без host-адаптера автономное
демо показывает сообщение об отсутствии подключения, не имитирует покупку
нажатием игровой кнопки. Для сквозной проверки есть отдельный browser host-драйвер;
подробности — в контракте выше. Правила XP/API 1.3 не менялись.
Незавершённые ML-изменения не включены в мобильный фикс.

Проверенный код: `fba79ce`. Чистый снимок: полный pytest без ошибок,
83 клиентских теста, 16 API-flow на каждом движке, TypeScript, экспорт всех
трёх платформ и browser smoke. [Таблица и ограничения](reviews/2026-09-07-pr9-fix-plan.md#4-реализация-и-проверка).

## История: mobile-polish@72410ac и API метрик

Источник UI — `feat/mobile-polish@72410ac`; источник системных правил —
`experiment/vxofi`. Визуал, ассеты, кнопки и порядок экранов менять только
после согласования с vxofi. [Карта merge и проверок](reviews/2026-09-06-mobile-polish-integration.md).
[Последний инкремент и итоговые проверки](reviews/2026-09-06-mobile-metrics-release.md).

Все экраны источника сохранены. В `DemoContext` новый `persistPlan` и
`savePlanAndCook` адаптированы к активному `user_id` и поколению сессии:
устаревший ответ не открывает чужую кухню; retry повторяет тот же план.
Короткий путь не создаёт чек и не даёт XP за открытие экрана готовки.

Прежняя локальная работа сохранена в `170c828`. Её UI обоев не подключён
к новым экранам; ниже его описание — история, не инструкция добавить его обратно.
Demo-селектор согласован отдельно (UI1): только внизу «Профиля», с подписью
«Только для демонстрации — синтетические покупатели». UI2 (будущая цель) не согласован.
Десять предметов коллеги пока выбираются
локально и не используют старый `home-decoration` API. Серверное сохранение
нового каталога и согласование лестницы наград остаются отдельными пунктами.

В тестах появился `mobile/tests/fixtures/mobile-polish-visual-baseline.json`:
контроль исходников экранов, компонентов, геометрии, темы и ассетов без доступа
к GitHub. Не обновлять хеши ради зелёного теста: сначала согласовать изменения.
Это контроль сохранения исходников, не доказательство UX-качества.

Новые карточки на `/recipes`, `/recipe`, `/products` перенесены без изменений
из `72410ac`. В baseline исходные хеши коллеги отделены от `approved_overrides`
для согласованного demo-блока; не перезаписывать весь эталон текущей копией.

## Read-only API для будущих дашбордов

[Контракт, формулы и ограничения](metrics-api.md): `/api/v1/metrics/summary`
и `/api/v1/metrics/daily`. Только синтетические данные процесса, без dashboard UI.
Чеки, дни покупок, суммы и стадии заданий разделены; выдача offer не является
показом в интерфейсе. Нет доказанного uplift, реальной маржи или ITT-метрик
без регистра участников эксперимента. Это дополнение API, не изменение XP.

## История: волна C1–C4 после merge PR #7

PR #7 слит в `main@05cc691`. Поверх него первоначально подготовлены локально,
затем включены в checkpoint `170c828` и текущую интеграцию:
три cooking-профиля с историей и изоляцией состояния в mobile; совместимые
исправления `70f3af5`; автономный safety-показ; экспорт конечной meal-выдачи.
[Карта задач и поля решений](poc-execution-plan.md),
[результат общей проверки](reviews/2026-09-06-poc-wave-1.md).

Frontend: `recommendationRequest.ts` → активный profile → все endpoint/user IDs;
`DemoContext` отсекает запоздавшие ответы старой сессии. Прежние default helpers
сохранены. Backend referral: валидный код получать через
`app.referral_codes.issue_invite_code(inviter_user_id)`; произвольный код больше
не принимается. Действующие XP и meal API 1.3 не менялись.

Экспорт `scripts.serving_review_pack.py` использует meal HTTP handlers,
но не оценивает ответы и не проверяет готовку. `scripts/safety_demo.py` показывает
другие требования отдельным процессом. Команды и границы — в их инструкциях.
GitHub CI PR #7 проверял предыдущую базу, не эту последующую волну.

## История: оформление C6 до mobile-polish (старый UI сейчас отключён)

[Контракт](home-decoration-contract.md) → `app/home_decoration.py` (demo-каталог),
`state.py` (сохранение/проверка ledger) → `main.py` (три новых endpoint) →
`mobile/src/api/endpoints.ts` → `DemoContext` → `HomeDecorationSheet` и
`KitchenWallpaper`. Разделены подтверждённое оформление и временный предпросмотр.
Уровни и право применения приходят с сервера; XP не расходуются и не выдаются
за выбор. API 1.3 и прежние meal-поля не меняются.

Первый элемент — обои: три новых варианта со второго уровня, по одному с
третьего и четвёртого. Это редактируемые demo-настройки, не измеренный баланс.
Исходный PNG кухни не изменён; кодовый слой обоев обходит силуэты предметов.
Добавление гарнитура, пола или полноценного магазина не включено автоматически.

Клиентские GET/мутации отсечены поколением запроса и покупательской сессии;
смена профиля не переносит чужую цель. Важно переносить provider, UI и новые
API-методы вместе. Backend остаётся process-local; перезапуск обнуляет и декор.
[Проверки второй волны и оставшиеся D3–D4](reviews/2026-09-06-poc-wave-2.md).

## Исправления ревью и API 1.3, 06.09

Прочитать [ADR-005](decisions/005-game-tasks-and-xp.md): выбранное игровое
задание + подходящая подтверждённая покупка + завершение дают право на 20 XP;
автоматические 10 XP за день покупки отменены. Предел — один бонус на покупочный день.
Три цели используют существующие current/repeat/explore; classifier и новые
мини-игры не нужны. Контракт сохраняет осмысленный вход после кассы.

**Что переносить вместе:** backend `app/contracts.py`, `meal_offer.py`, `state.py`,
`main.py`, `meal_plan.py`, `progress.py` и mobile API/types/domain/state/screens.
Сохранение плана теперь требует полученный из meal-выдачи `offer_id`; нельзя
использовать случайный/статический token. `plan.reward` отделён от completion,
`progress.rewarded_meals` — от количества приготовлений. Подробности — §8 ADR-005.

`60d4806` исправляет три замечания: duplicate-чек возвращает собственный связанный
план; магазин выбирается с совместимым способом получения; legacy-радиус до 100 км
не падает, новый shopping_context по-прежнему ограничен 20 км. Необязательный
ингредиент больше не блокирует обязательную корзину.

**Проверки:** новые `tests/test_game_rewards.py` и `test_review_regressions.py`;
mobile API smoke включает настоящий provider с потерей первого ответа и
post-checkout награду. Текущий demo-чек явно регистрируется в receipt endpoint,
без passive XP; это синтетическое доказательство, не промышленная интеграция.
Новый ручной сценарий — [mobile README](../mobile/README.md).

Дополнение после повторного ревью: опорный день cook-награды считается по первой
полной корзине во времени среди всех подходящих чеков до завершения, включая
перекрывающиеся. После завершения основание не меняется. В post-checkout ready
клиент не предлагает «повтор покупки», если собственного события для повтора нет.
Код — `state.py::_apply_plan_receipt` и `DemoContext.pendingReceipt/canConfirmPurchase`.

Публикация и итоговые checks фиксируются в
[плане закрытия ревью](reviews/2026-09-06-fixes-and-merge.md). По указанию `vxofi`
независимые ревью агентов заменяют дополнительное ожидание коллег; merge — после
исправлений и зелёных checks. Новые сцены/декор и ручной телефонный smoke не заявлены.

**Далее — история API 1.2 и прежних проверок, не актуальные правила XP.**

## Начать здесь: обновление после PR #8

06.09 ML-ветка `experiment/recsys-benchmark@02132c8` попала в `main@372de69`
до слияния нашего PR #7. Наш `fc01b94` сохранён как первый родитель;
новый main объединён в `experiment/vxofi`, не откатан и не переписан.
**PR #7 остаётся точкой ревью; merge в main ждёт ShmelJUJ.**

План обновления:

- [x] Сверить 12 конфликтующих файлов и сохранить рабочий API 1.2.
- [x] Перенести исправления runtime scorer: 37 замороженных обучающих рецептов,
  исключение сигнала наличия из score по умолчанию, отдельные ID обучения,
  история строго в прошлом, фиксированный пул сохранённых рецептов при обучении.
- [x] Сохранить исследовательский стенд в исполнимом виде, не включив его
  экспериментальные политики в приложение; восстановить исходные проверки.
- [x] Проверить четыре ML-манифеста, оба мобильных движка, клиентские тесты
  и Expo export. Итог общего pytest указан в PR.
- [ ] Ревью ShmelJUJ и разбор блокирующих замечаний перед merge.

Публикация и удалённые checks отслеживаются в [PR #7](https://github.com/x5-loyalty-hackathon/x5_loyalty_system/pull/7):
зелёные checks старого `fc01b94` не подтверждают новое объединение.

Локальная приёмка обновления: **375 Python-тестов прошли, 3 skipped**
(2 требуют необязательного gradient booster, 1 fixture не содержит сохранённых
рецептов); **28 клиентских**, **6 TS/API на mock + 6 на model без fallback**,
TypeScript и Expo export web/iOS/Android — успешно. Новые шесть проверок границ
входят в 375. Это не ручной прогон на телефоне и не проверка качества на людях.

Что важно коллегам:

- `app/contracts.py`, `service.py`, safety/state и **весь mobile** сохранены
  относительно `fc01b94`. Чек, HOME, одна точка, book/repeat, cook/ready,
  явный full-basket выбор и XP не перепроектированы.
- Runtime `recsys/model.py` ранжирует каталог запроса (в demo три блюда).
  Общий каталог остаётся 47 рецептов / 98 ингредиентов, но обучение по умолчанию
  использует **37 baseline**, остальные 10 — кандидаты. Ни наличие, ни каталог
  текущего запроса не переобучают модель. Scorer не заменяет финальный selector.
- [recsys/experimental/README.md](../recsys/experimental/README.md) — карта
  совместимости ML-стенда: его контракты/политики/генераторы находятся отдельно.
  CLI `recsys.benchmark`, `panels`, `human_eval`, `experiment1/2/3_*` сохранены;
  импорты и исходные ML-тесты переключены на исследовательские версии.
- Рецепты/ингредиенты/metadata/JSON не дублируются. Неизменность исходных
  панелей проверяется по манифестам без их перезаписи. Готовые карточки human
  pilot сохранены; реальные ответы людей не собирались при интеграции.
- `app/impression_log.py` сохранён как схема/оценщик исследовательского лога,
  **не подключённый HTTP endpoint или сбор реальных показов**.
- Исходные benchmark-числа не перезаписывались. EXP-002/003/004 — отдельные
  `proposed` материалы, не замена принятых ADR-002/003/004.
- Примеры `recsys/examples/sample_recommendations.json` пересозданы на новом
  runtime scorer через API 1.2; исторический backup коллеги остаётся историческим.

Новые регрессии — [test_recsys_merge_boundaries.py](../tests/test_recsys_merge_boundaries.py):
score не зависит от инвентаря, заморожено обучение, нет будущих чеков,
HTTP-схема не принимает исследовательские поля, настоящий backend не импортирует
экспериментальный контур, исходные каталожный хеш и четыре манифеста совпадают.

Защиту `main` попытались включить: 1 review + свежие backend/mobile checks,
без force push/удаления. GitHub ответил 403: функция недоступна для приватного
репозитория на текущем тарифе. Настройки доступа и тариф не меняли; до отдельного
решения команда соблюдает запрет merge без ревью вручную.

**Ниже — история первой интеграции 05.09.** Фразы «не перенесено» и числа
обучающих рецептов относятся к тому этапу; актуальные границы описаны выше.

## Общий main: что добавлено после первой интеграции

Источник — `main@8be7a2d` / `mobile@b948b37`. Слияние сохраняет историю обеих
веток и системные решения API 1.2, а не возвращает legacy-клиент из mobile.

- Приняты уточнённый `/data/` ignore, исходники/спрайты и новая раскладка кухни
  с запасным рисунком при отсутствии подходящего. Подписи товаров остаются
  точными; рисунок может быть условным, это явно написано на экране.
- **Покупка наполняет кухню.** В `DemoContext` сохраняются `kitchenItems` и
  обработанные ID чеков; `domain/kitchen.ts` сопоставляет выбранные SKU с
  ингредиентом рецепта. Товар без сопоставления показывается под своим SKU,
  готовое блюдо — отдельной позицией, не набором сырых ингредиентов.
- От исходного `6c70677` отличается момент изменения: только после
  `verified/duplicate` от API. При `rejected/pending_review`, неизвестном статусе
  или сетевой ошибке кухня не меняется. Потерянный ответ восстанавливается
  повтором того же чека; известный чек второй раз не применяется.
- Корзина сохранённого плана остаётся для повтора запроса. После готовки
  упаковки автоматически не удаляются. Это история покупок текущей сессии,
  не серверный холодильник и не новые признаки наличия для recommender.
- При конфликтах сохранены наши meal-экраны, книга, «Мой план», ошибки,
  исправленная `KitchenSheet` и API 1.2. Старые `data/demo.ts` — только
  дизайн-референс; backend/ML от этого объединения не меняются.

Приёмка объединённого кода: **125 Python, 28 клиентских, 6 TS/API × 2 движка**,
TypeScript и Expo export web/iOS/Android. Новые пять клиентских регрессий
проверяют наполнение кухни, повторы/отклонения/сеть, ready и раскладку спрайтов.
CI предыдущего `1492019` прошёл; новая объединённая версия проверяется отдельно
перед merge. Успешный CI не заменяет ручной прогон на телефоне.

После слияния интеграционного PR командная база — `origin/main`; коллегам
передаём ссылку на **этот файл в main**. Автор продолжает работу в своей ветке.
Ниже сохранена карта первого переноса: она объясняет, почему часть старого
mobile/ML-кода намеренно не стала контрактом общей системы.

## План этой итерации

- [x] Перенести кухню и графику `feat/mobile-app@4f374c3` поверх meal-flow.
- [x] Исправить жест панели; готовка завершается только через meal-план.
- [x] Адаптировать каталог и scorer `recsys-benchmark@c28b613` к API 1.2.
- [x] Проверить прежние инварианты и сквозной mobile/API flow на новом scorer.
- [x] Дописать карту кода, отличия от исходных веток, команды и результаты.

## Что переносим и что сохраняем

| Источник | Берём | Не подменяем |
|---|---|---|
| `feat/mobile-app@4f374c3` | Спрайты, слоты кухни, сцену, выдвижную панель и представление готовки | Наши книгу, план, чек выбранных SKU, обработку ошибок; локальный pantry не становится фактом наличия |
| `experiment/recsys-benchmark@c28b613` | Каталог ингредиентов/рецептов, metadata, новый ML scorer и нужные ему модули | API 1.2, safety, home, single-store, selector, XP, готовые блюда через проверенные остатки |
| `experiment/vxofi@658ce45` | Основа интеграции, mobile/API-тесты и решения ADR-001–004 | Не откатываем на API 1.0 ради готового кода соседней ветки |

Это **выборочный перенос**, не объявление всей ML-ветки слитой. Её benchmark,
CatBoost, альтернативные ranking policies, LLM-аудит и отчёты остаются отдельной
работой. Они не считаются результатами новой общей системы без повторного
прогона на ней. Ссылки на исходные коммиты сохраняются для сравнения.

## Границы, которые важно не потерять

- «Приготовил» — `complete-cook`, не синтетический чек от имени пользователя.
- Покупка подтверждается выбранными SKU. Повтор запроса не даёт новые XP.
- Недавний чек не является точным холодильником или источником срока годности.
- Уценка — явный выбор; нет брони. Дом/работа — разные demo-расстояния.
- `current/repeat/explore` и порядок карточек определяет наш selector, не UI.
- Каталоговая пара «рецепт — готовый товар» не доказывает наличие и безопасность.
- Время/порции/кухня рецепта сохраняются как metadata, не требуют отката API.
- ML обучен на синтетике. Его подключение не доказывает 70% на людях или uplift.

## Что именно изменилось относительно исходных веток

### Frontend: `feat/mobile-app@4f374c3`

- Перенесены `KitchenScene.tsx`, `KitchenSheet.tsx`, `kitchenSlots.ts`,
  `kitchenPlacement.ts`, `productSprites.ts` и все 14 PNG из `assets/products/`.
  Сцена и позы сохранены; неработающая кнопка меню скрыта, пока нет обработчика.
- `KitchenSheet`: сохранённый `PanResponder` читает актуальные размеры и callback
  через ref. Раньше после `onLayout` жест оставался ограничен начальной высотой
  201; теперь ограничение обновляется. Есть также открытие кнопкой и обработка
  прерванного жеста. В нашей компоновке начальная граница — 221.
- `index.tsx` использует новую сцену, но вход перед доставкой и текущий meal-flow
  остаются нашими. `KitchenProduct` — отдельный тип отображения, без импорта
  старого локального каталога. Неразмещённые картинки не удаляют позиции из списка.
- `products.tsx`: маркировка уценки и зачёркнутая исходная цена взяты как приём
  оформления, но сами SKU/цены — из ответа API, не из `data/demo.ts`.
- Готовка: **план → «Начать готовить» → кухня с шагами → «Я приготовил» → сервер**.
  `DemoContext.confirmCooking()` возвращает успех/неуспех; переход к прогрессу —
  только при `completed/duplicate`. При ошибке можно повторить запрос.
  Фиксированный чек и удаление целых упаковок из «холодильника» не перенесены.
- Нижняя вкладка «Мой план» сохранена. В соседней ветке удалён старый «Каталог»,
  но в нашей этот маршрут уже обслуживает выбранный meal-план.

### ML: `experiment/recsys-benchmark@c28b613`

- Перенесены `catalog.py`, `recipes.py`, `model.py`, `pantry.py` и необходимый
  для импорта `data/price_reference.json`. Каталог: **47 рецептов, 98 ингредиентов**;
  scorer: логистическая регрессия с **16 признаками**, в том числе наблюдаемыми
  характеристиками истории и взаимодействиями пользователя с рецептом.
- `recipes.py`: `servings/dish_type/cuisine` сохранены в новом
  `recipe_metadata.py::RECIPE_METADATA`, не добавлены неоговорёнными полями в
  строгую API-схему. `quantity/unit/required` используют существующий контракт;
  `meal_intent_id=recipe_id` даёт стабильный ключ, но сам не создаёт ready-SKU.
- `model.py` и `pantry.py`: явно заданные продукты дома уменьшают докупку,
  но не превращают `explore` в `current`; готовое блюдо не разбирается на сырые
  продукты из его состава. В missing count/cost входят только обязательные
  ингредиенты. История покупок автоматически не превращается в остатки дома.
- Serving использует `DISABLED_PANTRY` и `include_effort_features=True`.
  Альтернативные настройки в классе — экспериментальные возможности исходника,
  не включённые решения продукта. Окончательный порядок и объяснения остаются в
  нашем service/selector, а не берутся напрямую из model reason codes.
- Наши `profiles.py`, `inventory.py`, `evaluation.py`, `simulation.py` и
  `economics.py` не заменены версиями из ML-ветки. Новая модель обучается при
  запуске на **наших** синтетических профилях с расширенным каталогом; это не
  воспроизведение всего benchmark коллеги.
- `recsys/examples/sample_recommendations.json` пересоздан текущим генератором:
  10 профилей проходят через настоящий `RecommendationService` и API 1.2.
  Это примеры recipe-выдачи, не лог кликов пользователя и не отчёт о causal uplift.
- `pyproject.toml` включает `recsys/data/*.json` в устанавливаемый пакет: иначе
  локальный editable-запуск работает, а wheel теряет обязательный price reference.

Не перенесены API 1.0, альтернативные ranking policies, CatBoost/benchmark,
историческая модель «истинного холодильника», LLM-аудит, ready-каталог как
источник наличия, калибровочные скрипты и старые отчёты. Агрегат цен — лишь
reference исходного эксперимента, не текущая цена в магазине. В API 1.2 нет
поля `price_is_estimate`; все наши inventory/мобильные цены явно демонстрационные.
Внешние датасеты, платные LLM-вызовы и новые ML-зависимости для serving не нужны.

## Карта кода: куда идти за изменением

Пути ниже — от корня репозитория. Распределение участков прежнее, не новые роли.

| Задача | Точка входа и граница ответственности |
|---|---|
| Картинка кухни, продукты, жест | [`KitchenScene`](../mobile/src/components/KitchenScene.tsx), [`KitchenSheet`](../mobile/src/components/KitchenSheet.tsx), [`kitchenPlacement`](../mobile/src/data/kitchenPlacement.ts); не менять серверный прогресс из UI |
| Навигация и состояние готовки | [`index.tsx`](../mobile/src/app/index.tsx), [`products.tsx`](../mobile/src/app/products.tsx), [`DemoContext`](../mobile/src/state/DemoContext.tsx); `run` блокирует двойные действия и возвращает результат |
| Запрос → товары → план → чек | [`endpoints.ts`](../mobile/src/api/endpoints.ts), [`mealFlow.ts`](../mobile/src/domain/mealFlow.ts); цены, store и SKU только из API |
| Формат данных и итоговая выдача | [`contracts.py`](../app/contracts.py), [`service.py`](../app/service.py), [`explanations.py`](../app/explanations.py); API 1.2, ADR-003/004, missing-first, safety и объяснения |
| Оценка релевантности | [`model.py`](../recsys/model.py): `compute_user_stats`, `compute_features`, `MLRecommendationEngine.rank`; вход/выход `RecommendationEngine` не менять |
| Расширение рецептов | [`catalog.py`](../recsys/catalog.py), [`recipes.py`](../recsys/recipes.py), [`recipe_metadata.py`](../recsys/recipe_metadata.py); metadata не означает поддержку порций в UI |
| События/XP/антифрод | [`main.py`](../app/main.py), [`state.py`](../app/state.py), [`safety.py`](../app/safety.py); авторитетное состояние на backend, без локального начисления |

### Что сейчас увидит человек в мобильном демо

Модель обучена на 47 рецептах, но **ранжирует каталог из запроса**. Мобильный
[`mealRequest.json`](../mobile/src/fixtures/mealRequest.json) пока содержит
одного фиксированного пользователя и три блюда: пасту с томатами, болоньезе и суп.
Истории чеков `purchase_history` в этом fixture нет: это также проверка cold start,
не демонстрация всех обучающих персон. `recipeDetails.ts` даёт шаги только для
этих трёх точных составов. Никаких локальных дополнительных карточек поверх API нет.
Продукты на рисунке начинаются с фиксированного недавнего чека и пополняются
принятыми API покупками текущей сессии. Это не меняет входные признаки
рекомендаций автоматически и не доказывает, сколько продукта осталось дома.

Чтобы расширить demo, нужно согласованно обновить профиль, recipe/ingredient IDs,
остатки обычных и ready-SKU, затем инструкции и тесты — не просто импортировать
47 названий в React. Каталог готовой еды без состава, срока и остатка в конкретной
точке не проходит вместо проверенного `InventoryProduct`.

## Как проверить после своих изменений

Backend из корня, после установки `python -m pip install -e '.[dev]'`:

```bash
.venv/bin/python -m pytest
.venv/bin/python -m recsys.generate_examples
git diff -- recsys/examples/sample_recommendations.json
```

Генератор **перезаписывает** tracked-пример; при неизменных model/data diff пуст.
После осмысленного изменения модели diff нужно просмотреть и сохранить, а не
отключать `test_committed_sample_artifact_matches_current_response_contract`.

Frontend из `mobile/`, Node 24, зависимости по lockfile:

```bash
npm ci --legacy-peer-deps --ignore-scripts --no-audit --no-fund
npm run typecheck
npm test
npm run test:api
X5_TEST_ENGINE=model npm run test:api
npm run export:check -- --max-workers 2
```

`test:api` запускает Python TestClient и настоящие TypeScript-сборщики, не HTTP
заглушку. В model-прогоне `/health` должен вернуть `model_fallback=false` и
`recommendation_engine=model`; незаметный переход на mock ломает тест.
Запуск приложения и сценарий ручной проверки — в [mobile/README](../mobile/README.md).

Локальный результат 05.09: **125 Python-тестов, 23 клиентских теста,
6 TS/API-тестов × 2 движка**, TypeScript и Expo export web/iOS/Android — успешно.
Новые регрессии: [`test_model_api12_integration.py`](../tests/test_model_api12_integration.py),
[`kitchen.test.mjs`](../mobile/tests/kitchen.test.mjs),
[`cooking-state.test.mjs`](../mobile/tests/cooking-state.test.mjs).
Также проверена одинаковая model-выдача в процессах с разными `PYTHONHASHSEED`.
CI дополнен model-прогоном; удалённый результат ещё не проверен.
Python wheel собран; из распакованного пакета вне рабочего каталога успешно
запущен `/health` с model без fallback и загружены все 47 рецептов.

Это тесты контрактов, бизнес-переходов и отдельных React-обработчиков с test doubles.
Expo export не является native build, автоматическим нажатием по экранам или
визуальной проверкой. Ручной прогон на телефоне остаётся открытым gate.

## Как продолжать в соседней ветке

1. Прочитать этот файл, [текущий статус](project-status.md) и ADR своего участка.
   После слияния интеграционного PR выполнить `git fetch origin`.
2. До слияния посмотреть `git log --oneline HEAD..origin/main` и
   `git diff --stat HEAD...origin/main`. Для конкретного файла
   полезно сравнение двух версий, например:

   ```bash
   git diff c28b613 origin/main -- recsys/model.py
   git diff b948b37 origin/main -- mobile/src/state/DemoContext.tsx
   ```

3. Сохранить свою незакоммиченную работу, затем в **своей** ветке интегрировать
   `origin/main`. Это не команда переключить всем ветку и не push в main.
   История mobile включена; ML переносился выборочно, поэтому merge его
   исходной ветки всё ещё может конфликтовать. Не разрешать конфликт заменой API/service/context
   старым файлом целиком; использовать карту адаптаций выше.
4. Прогнать проверки своего участка и оба TS/API smoke. Если меняется контракт —
   сначала согласовать решение и обновить ADR, затем код обеих сторон.

## Следующий шаг, не скрытая часть завершённого scope

- Совместный ручной прогон на телефоне и визуальная доводка новой кухни.
- С ML-участником — расширение mobile fixture/персон и повторный eval конечной
  API 1.2 выдачи на 30–50 профилях с baseline и разделением cooking/ready сегментов.
  Старые benchmark-числа и текущие тесты связности не заменяют этот deliverable.
- Поправка «закончилось», декор, UI рефералов и приватного ранга — отдельные задачи.
- Персистентность, реальные остатки/доставка и серверная привязка плана к ранее
  выданной рекомендации пока не реализованы. Не обещать их в демонстрации.
